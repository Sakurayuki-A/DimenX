name: dimenx
description: A Flutter-based anime streaming application for Windows desktop.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  html: ^0.15.4
  cached_network_image: ^3.3.0
  video_player: ^2.8.1
  chewie: ^1.7.4
  media_kit: ^1.2.1
  media_kit_video: ^1.2.5
  media_kit_libs_windows_video: ^1.0.11
  media_kit_native_event_loop: ^1.0.9
  flutter_inappwebview: ^6.0.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  flutter_staggered_grid_view: ^0.7.0
  url_launcher: ^6.2.1
  window_manager: ^0.3.7
  bitsdojo_window: ^0.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
